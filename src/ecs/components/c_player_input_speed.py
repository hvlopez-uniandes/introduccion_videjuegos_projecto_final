class CPlayerInputSpeed:
    """Velocidad en píxeles por segundo para las flechas (input_velocity del player.json)."""

    def __init__(self, pixels_per_second):
        self.pixels_per_second = float(pixels_per_second)
